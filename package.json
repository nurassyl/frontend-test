{
  "name": "frontend-test",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "author": "Nurasyl Aldan <nurassyl.aldan@gmail.com>",
  "scripts": {
    "clear-dist": "rm -rf ./dist",
    "clear-all": "rm -f *.log* && rm -f package-lock.json && rm -f yarn.lock && rm -rf node_modules && npm run clear-dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run clear-dist && npx gulp --env=development",
    "build": "npm run clear-dist && npx gulp --env=production",
    "server": "node server.js"
  },
  "engines": {
    "node": "^8"
  },
  "os": [
    "linux",
    "win32"
  ],
  "cpu": [
    "x64"
  ],
  "devDependencies": {
    "bower": "^1.8.4",
    "browser-sync": "^2.23.6",
    "express": "^4.16.3",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^5.0.0",
    "gulp-cli": "^2.0.1",
    "gulp-coffee": "^3.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-csso": "^3.0.1",
    "gulp-htmlmin": "^4.0.0",
    "gulp-less": "^4.0.0",
    "gulp-plumber": "^1.2.0",
    "gulp-preprocess": "^2.0.0",
    "gulp-pug": "^4.0.1",
    "gulp-sourcemaps": "^2.6.4",
    "gulp-uglify": "^3.0.0",
    "gulp-util": "^3.0.8"
  }
}
